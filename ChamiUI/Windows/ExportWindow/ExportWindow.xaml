<Window x:Class="ChamiUI.Windows.ExportWindow.ExportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChamiUI.Windows.ExportWindow"
        xmlns:vm="clr-namespace:ChamiUI.PresentationLayer.ViewModels"
        d:DataContext="{d:DesignInstance vm:ExportWindowViewModel, IsDesignTimeCreatable=True}"
        mc:Ignorable="d"
        Title="Export environments" Height="450" Width="400">
   
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" 
               Grid.Column="0" 
               FontSize="20" 
               Content="Export environment" 
               FontWeight="DemiBold" 
               HorizontalContentAlignment="Center"/>
        <GroupBox Grid.Row="1" Grid.Column="0" Header="Export settings:">
            <StackPanel>
                <RadioButton Content="All environments" IsChecked="{Binding ExportAll}"/>
                <RadioButton Content="Selected environments" IsChecked="{Binding ExportSelected}"/>
            </StackPanel>
        </GroupBox>
        <ListView Height="200" Grid.Row="2" Grid.Column="0" ItemsSource="{Binding Environments}" IsEnabled="{Binding ExportSelected}">
            <!--TODO Scrivre l'ItemTemplate-->
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <Label Content="{Binding Name}"></Label></StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>

<UserControl x:Class="ChamiUI.Controls.ApplicationDetectorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ChamiUI.Controls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300" Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <GroupBox Grid.Row="0" Grid.Column="0" Header="Detect applications">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition MinHeight="200" Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" 
                            Grid.Column="0" 
                            Orientation="Horizontal">
                    <CheckBox HorizontalContentAlignment="Center"/>
                    <Label HorizontalContentAlignment="Center" Content="Enable detecting the following applications are running"/>
                </StackPanel>
                <StackPanel Grid.Row="1" 
                            Grid.Column="0" Orientation="Horizontal">
                    <Label Content="App name"></Label>
                    <TextBox Width="150"/>
                    <Button Content="Add" x:Name="AddApplicationButton" Click="AddApplicationButton_OnClick"></Button>
                </StackPanel>
                <ListBox Grid.Row="2" Grid.Column="0"></ListBox>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
